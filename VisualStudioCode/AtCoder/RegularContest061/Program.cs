using System;

namespace RegularContest061
{
    class Program
    {
        static void Main(string[] args)
        {
            String str = Console.ReadLine();
            int len = str.Length;

            int num = int.Parse(str);
            //it exists in all pattern 
            //  when the number of 'plus' is zero.
            int sum = num;

            for()
        }

        class division
        {
            
        }
    }
}
